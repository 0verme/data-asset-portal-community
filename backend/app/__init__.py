"""Backend application package.

The production composition root is ``backend.asgi:app``.  FastAPI adapters,
application primitives, services and database providers are imported from
submodules; the retired Flask factory is intentionally no longer part of this
package API.
"""
