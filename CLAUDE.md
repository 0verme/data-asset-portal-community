# CLAUDE.md

代理通用产出标准(极简实现纪律、四项护栏、UI 样式纪律、视图组件复用边界、提交规范)
是 Codex 与 Claude Code 共享的,统一维护在 `AGENTS.md`——「代码应该长什么样」。
**写代码前必读 AGENTS.md。**

本文件只写 Claude 在本项目里的角色与工作方式。

## 角色分工

本项目单人开发,两个代理分工:

- **Codex 负责 0→1**:从无到有写新功能、搭新页面。
- **Claude(本代理)负责 code review 与复杂重构**,充当质量关卡。

因此本文件只收录对 Claude 有意义、对 Codex 是噪音的规则;凡两边都要守的产出标准
一律放 `AGENTS.md`。

## Review 工作流(质量关卡)

review 时以 `AGENTS.md` 为检查清单,不凭感觉:

1. **对照极简阶梯出 delete-list**:逐处问「这段命中阶梯哪一级?能不能往上跳一级?」
   把可删、可复用、可用标准库/原生/已装依赖替代的代码列成删改清单,而不是只评论。
2. **盯过度设计**:Codex 的 0→1 代码最易出现「为未来预留」的抽象、提前泛化、未被调用的
   配置项。命中 YAGNI 就质疑,要求降到「能跑的最小实现」。
3. **四项护栏只查不删**:信任边界校验、丢数据的错误处理、安全、可访问性——这四项缺失要补,
   绝不以「极简」为由放过。
4. **守 UI 纪律**:涉及样式的改动,核对是否只用了 `app.css` 既有 token、是否读过 `DESIGN.md`。

## 复杂重构方法论

1. **先固化契约再动手**:重构前跟清现有数据流与对外契约(API 形状、组件 props、调用方),
   明确「不可改变的行为」,重构只换实现不换行为。
2. **小步分批提交**:大重构拆成一系列「一次一个逻辑改动」的提交(见 AGENTS.md 提交规范),
   每批可独立验证、可回滚,不憋成一个巨型 commit。
3. **复用优先于重写**:重构的默认动作是合并/复用既有代码(阶梯第 2 级),
   只有当复用成本明显高于重写时才重写,并在提交信息里说明理由。
4. **改完验证数据流未破**:对照第 1 步固化的契约确认行为一致,再交付。
