# AGENTS.md

本文件是 Codex 与 Claude Code 共享的代理规则**单一事实源**,定义「代码应该长什么样」——
两边都要守的产出标准。Claude 专属的工作方式(review 流程、重构方法论)见 `CLAUDE.md`。

## 极简实现纪律(judgment ladder)

本纪律仅在**编写实现代码**时生效:先理解问题、读相关代码、跟清真实数据流之后,
再选层级(不适用于必要的测试、文档、错误处理)。

写代码前,按顺序逐级自问,命中即停,不再往下:

1. **这功能需要存在吗?** — 不需要就不做(YAGNI)
2. **本仓库已有吗?** — 复用,别重写
3. **标准库能做吗?** — 用标准库
4. **平台/框架原生特性能做吗?** — 用原生(React/Flask 已提供的能力优先)
5. **已装依赖能做吗?** — 用现有依赖,别引新包。
   本项目硬约束:部署环境对新增依赖引入成本高,故**默认拒绝引新包**。
6. **一行能解决吗?** — 就一行
7. 以上都不行,才写「能跑的最小实现」

原则:代码小是因为**确实需要**,不是为压缩而压缩。可以对方案偷懒,
**绝不对读代码偷懒**。

**四项绝不省略**(不在精简范围内):信任边界的输入校验、可能丢数据的错误处理、
安全、可访问性。

## UI / 样式改动纪律

任何 UI/CSS 改动前必须先读 `frontend/DESIGN.md`,它是本项目 UI 修改的唯一风格依据。
只允许使用 `app.css` 中已有的 token,禁止新建颜色/圆角变量,禁止散落 hex。

## 视图组件复用边界

通用视图组件 CardGridView.jsx / GroupView.jsx(配置驱动、纯展示、render-* props)
仅服务「徽标 + 标题 + 描述 + 底部」结构相似的卡片场景,当前接入:数据仓库页(HomePage)、
指标维护页(IndicatorPage)、报表资产页(ReportPages)、上游卸数页(UpstreamPages)、
API 资产页(ApiAssetView)。

下游系统推送页【独立实现】,不接入通用组件。原因:其卡片结构更重(logo 主导头部 +
内联两行标题 + 结构化连接信息键值网格 + 进入入口),与通用组件的简单模型分歧大;
强行复用需新增 2~3 个条件渲染槽,导致组件膨胀且增加 HomePage/IndicatorPage 的回归负担,
属负收益。视觉一致性已靠同套 --ink/--accent/--surface token + 平行的 .sys-* class 达成,
无需代码复用。

判断准则:新增页面只有在同为「徽标+标题+描述+底部」结构时才考虑接入通用组件;
结构上更重/形态不同的卡片优先独立实现 + 共用 token/class。

## 提交规范

- 遵循 Conventional Commits(`feat:` / `fix:` / `chore:` / `refactor:` …)。
- 一次提交只包含一个逻辑改动,不混入无关变更。
