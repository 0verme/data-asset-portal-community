# 待确认事项

本文只记录当前代码尚未完成、仍需产品或架构决策的事项。已实现功能和历史改造过程由代码、测试及 Git 历史追溯。

## 1. 细粒度权限

当前固定角色为 `admin` 和 `maintainer`：管理员管理系统配置，维护员维护业务模块并读取操作日志。尚未实现按模块、数据域或具体操作授权的 RBAC。

## 2. 资产风险外部接入

资产详情已有 `assetRisks` 展示骨架，但外部审计结果导入、查询、持久化、实时校验和问题闭环尚未实现。边界与建议见 [资产风险与审计平台联动设计](./asset-risk-integration-design.md)。

## 3. 智能问数与语义推荐

当前只有关键词搜索和资产元数据底座，没有向量检索、Embedding、LLM 组装或可执行语义层。候选路线见 [智能问数底座与语义推荐路线](./semantic-recommendation-roadmap.md)。

## 4. 社区版发布准备

当前仓库代码与数据已按开源边界整理（演示数据统一来自 `demo/datasets/` 零售虚构数据集，
由 `demo/validate_demo_data.py` 全仓扫描守护）。若发布社区版，仍建议：

- 新建无历史的新仓库，按白名单迁移通用代码与中性演示数据（P3 范围）；
- 重新执行凭据、许可证、构建产物、样例数据与截图内容核验；
- ~~正式处理 lineage-viewer 依赖~~（已完成：P2 已将其作为 `frontend/packages/` 内置 npm workspaces 纳入仓库，不再依赖公共 npm registry）；
- 界面截图：旧 32 张内测截图已在 P2 移除，正式发布前用零售演示数据重拍核心 5~10 张后恢复 README 引用。
